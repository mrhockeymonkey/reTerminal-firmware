//! Native binary (design brief §9): holds the current `screen-spec`, serves
//! it as JSON to the device (`GET /screen`) and to the browser preview,
//! and accepts updates (`PUT /screen`). Runs on the developer's machine —
//! the local server the device's firmware polls over WiFi (design brief §10).

fn main() {
    println!("server: placeholder binary, not yet implemented — see design brief §9/§10");
}
