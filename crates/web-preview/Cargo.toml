[package]
name = "web-preview"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "wasm32 build of the render crate with a tiny C ABI, drawn to an HTML canvas by server/assets/preview.js"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
render = { workspace = true }
screen-spec = { workspace = true }
